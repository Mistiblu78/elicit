<!-- Every item MUST use the five-field format below. /build reads each item
     and relies on all five fields being present and consistently formatted.
     The header encodes methodology choices so /build doesn't re-ask. -->

# Build Checklist

## Build Preferences

- **Build mode:** Autonomous
- **Comprehension checks:** N/A (autonomous mode)
- **Git:** Commit after each checklist item with message: "Complete step N: [title]"
- **Verification:** Yes — one named checkpoint: after item 5 (API route), pause and display the raw JSON response from a real Claude API call. The agent stops here and waits for schema approval before proceeding to item 6. No other checkpoints unless something breaks.
- **Check-in cadence:** N/A (autonomous mode)

## Checklist

- [x] **1. Project setup & infrastructure**
  Spec ref: `spec.md > Stack` and `spec.md > Runtime & Deployment`
  What to build: Initialize a Next.js 15 project with Bun inside the repo root (`bunx create-next-app@latest . --typescript --tailwind --app`). Install all dependencies: `@anthropic-ai/sdk`, `docx`, `jszip`, `@opennextjs/cloudflare`, `wrangler`. Configure the `@opennextjs/cloudflare` adapter in `next.config.ts`. Create `wrangler.toml` with Cloudflare Pages config. Configure Tailwind brand tokens in `tailwind.config.ts`: navy `#1A4269`, light blue `#A3DDF2`, gray `#707070`, white `#FFFFFF`. Set up `app/globals.css` with Tailwind base styles. Create the full file structure scaffold — all directories and empty stub files per `spec.md > File Structure` (components, context, lib, data, public). `.env.local` already exists with `ANTHROPIC_API_KEY` placeholder — do not overwrite it. Verify `bun run dev` starts without errors.
  Acceptance: Dev server runs on localhost without build errors. All directories and stub files exist per spec. Tailwind brand color tokens are configured and available. `wrangler.toml` and `next.config.ts` reference `@opennextjs/cloudflare`.
  Verify: Run `bun run dev` and confirm localhost loads without errors. Check `tailwind.config.ts` for brand tokens.

- [x] **2. SessionContext**
  Spec ref: `spec.md > Session State`
  What to build: Create `context/SessionContext.tsx` with the full `SessionState` interface and `GeneratedDocument` interface exactly as defined in the spec. Set sensible defaults for all fields (empty strings, nulls, empty arrays, `apiCallCount: 0`, `isLoading: false`, `error: null`). Export a `SessionProvider` component and a `useSession` hook. Wrap the entire app in `app/layout.tsx` with `<SessionProvider>`. No UI — this is the state layer only.
  Acceptance: `context/SessionContext.tsx` exports `SessionProvider` and `useSession`. `app/layout.tsx` wraps children in `SessionProvider`. The full `SessionState` and `GeneratedDocument` interfaces are defined with correct TypeScript types matching the spec exactly.
  Verify: Run `bun run dev`. Confirm no TypeScript errors on the context file or layout.

- [x] **3. Page 1 — Landing**
  Spec ref: `spec.md > Page 1 — Landing > ComplianceNotice Component`
  What to build: Build `components/landing/ComplianceNotice.tsx` — three-point compliance notice: (1) the two-field approach — identifying info vs. placeholder-only case notes; (2) attorney review requirement before serving; (3) session-only data handling — no storage, no logs. Style with Tailwind using brand tokens. Notice must be visible above the fold without scrolling. Add a single "Get Started" CTA button that navigates to `/input`. Wire up `app/page.tsx` to render `ComplianceNotice`. No case information collected on this page.
  Acceptance: Landing page renders three-point compliance notice above the fold. "Get Started" button is the only CTA and navigates to `/input`. No form fields or case information on this page. Styling uses Elicit navy and brand colors.
  Verify: Run `bun run dev`, open localhost. Confirm notice renders without scrolling. Click "Get Started" — confirm navigation to `/input` (page may be empty at this stage).

- [x] **4. Page 2 — Input form**
  Spec ref: `spec.md > Page 2 — Input Form`
  What to build: Build all four input components and compose them in `app/input/page.tsx`. First, author the pre-filled case notes example text — a realistic 2–4 sentence Texas family law scenario using correct placeholder strings (`[PETITIONER]`, `[RESPONDENT]`, `[CHILD]`, `[COUNTY]`, etc.) that demonstrates proper placeholder usage to attorneys. Then build: `IdentifyingInfoForm.tsx` — all 16 identifying info fields (Petitioner name, Respondent name, Opposing counsel name, Cause number, Court, County, Requesting party, Opposing pronoun, Attorney name, Firm name, Firm address, Firm city/state/zip, Phone, Fax, Bar number, Email), wired to SessionContext, never sent to API; required fields highlight red on the field itself when Generate is attempted while empty. `CaseDetailsForm.tsx` — case type dropdown (Divorce — No Children / Divorce — With Children / Original SAPCR / Modification), Modification sub-type dropdown revealed only when Modification is selected and required before Generate is available (Conservatorship/Possession / Child Support / Both), discovery level dropdown (Level 1 / Level 2 / Level 3), request type checkboxes (Interrogatories / Requests for Production / Requests for Admissions / Requests for Disclosure), response deadline (30 days / 50 days), soft interrogatory warning (non-blocking, one line of helper text, appears when Interrogatories checked AND case type is Divorce — With Children, Original SAPCR, or Modification, disappears dynamically on case type change to Divorce — No Children). `CaseNotesField.tsx` — pre-filled with the authored example text, inline notice listing all 22 available placeholders, running character counter below field, inline warning at 2000 characters, validation message "Case notes must be 2000 characters or fewer." and Generate blocked when over 2000. `GenerateButton.tsx` — validates all required fields and at least one request type selected before enabling; on click changes to "Drafting your requests..." and becomes inactive; no page transition at click.
  Acceptance: All fields render and write to SessionContext. Modification sub-type reveals and hides conditionally. Soft warning appears and disappears dynamically. Character counter counts accurately. Generate button is disabled until all required fields are complete and at least one request type is selected. Required field errors highlight red on the field. Button enters loading state on click.
  Verify: Run `bun run dev`, navigate to `/input`. Test: select Modification — confirm sub-type appears. Check Interrogatories + Divorce With Children — confirm soft warning. Type over 2000 chars — confirm counter turns warning and Generate is blocked. Try Generate with empty required fields — confirm red highlights.

- [x] **5. API route, prompt construction & system prompt**
  Spec ref: `spec.md > API Route`, `spec.md > Architecture Overview`
  What to build: First, update `data/elicit_system_prompt.md` — add three conditional instructions for the `modificationType` field (one for Child Support only, one for Conservatorship/Possession only, one for Both) and update the output format section to instruct Claude to return a JSON array matching the `GeneratedDocument` schema from the spec. Then build: `lib/buildPrompt.ts` — pure function that takes the API payload and constructs the labeled user message (Case type, Modification type — set to `N/A` when not applicable, Discovery level, Request types, Response deadline, Case notes). `app/api/generate/route.ts` — validates incoming payload, returns `{ error: "session_limit" }` immediately if `apiCallCount >= 10` without calling Claude, loads `lib/prompts/elicit_system_prompt.md` in a Cloudflare Workers Edge-compatible way (configure raw file loader in `next.config.ts` or export as TypeScript string constant), calls Claude (`claude-sonnet-4-6`, `max_tokens: 4000`), parses the JSON response, returns the `GeneratedDocument[]` array. Handle all error cases: 429 → `{ error: "rate_limit" }`, session limit → `{ error: "session_limit" }`, Anthropic timeout/network → `{ error: "api_error" }` with HTTP 500. Copy `data/elicit_system_prompt.md` to `lib/prompts/elicit_system_prompt.md` as the server-side source of truth.
  Acceptance: POST to `/api/generate` with a valid payload returns a JSON array of `GeneratedDocument` objects. Session limit check returns the correct error without calling Claude. All three error responses are structured JSON. System prompt loads without errors in the Edge runtime.
  Verify: **⚠️ CHECKPOINT — STOP HERE.** Make a real POST to `/api/generate` with a test payload (e.g., Divorce — With Children, Level 2, Interrogatories + RFP, 30 days, sample case notes with placeholders). Display the complete raw JSON response in the conversation. Wait for approval of the schema before proceeding to item 6.

- [x] **6. PII layer & Page 3 shell**
  Spec ref: `spec.md > PII Architecture > Find-and-Replace System`, `spec.md > Page 3 — Output > DocumentTabs Component`
  What to build: Build `lib/replacePlaceholders.ts` — pure function implementing the full replace map from the spec (all 22 placeholders → SessionContext values, `[DATE]` → `new Date().toLocaleDateString('en-US')`). Applied to every string field in a `GeneratedDocument` after the API responds. Build `app/output/page.tsx` — reads `documents` from SessionContext, applies `replacePlaceholders` to every string field in each document, renders `DocumentTabs`. Build `components/output/DocumentTabs.tsx` — one tab per document in the `documents` array (only selected types appear), tab labels: Interrogatories / Requests for Production / Requests for Admissions / Requests for Disclosure, active tab indicator in Elicit navy `#1A4269`. Wire `GenerateButton` to POST `/api/generate` with the full payload from SessionContext, store the response in `SessionContext.documents`, increment `apiCallCount`, and navigate to `/output` on success.
  Acceptance: After clicking Generate on Page 2, app navigates to `/output`. Tabs show only the selected request types. No placeholder strings are visible — all are replaced with real values from the identifying info form. Tab switching works correctly.
  Verify: Run a real generation flow from Page 2. Confirm navigation to Page 3. Confirm tabs match selected request types. Confirm real names (not placeholders) appear in the document content.

- [x] **7. Document viewer & output controls**
  Spec ref: `spec.md > Page 3 — Output > DocumentViewer Component` through `spec.md > Page 3 — Output > Start New Session`
  What to build: `DocumentViewer.tsx` — paper-like card displaying the full formatted document for the active tab: caption block (centered, Texas court format), document title, To line, cover paragraph, definitions section (Interrogatories and RFP only), numbered requests under bold section headings, signature block, certificate of service. Bottom fade CSS gradient indicating scrollable content. `ReviewCheckbox.tsx` — displayed once above the tabs, unchecked on load, checkbox label: "I have reviewed these documents and accept responsibility for their accuracy before serving." `DownloadControls.tsx` — "Download [Type]" primary button with dynamic label matching active tab, "Download all (.zip)" secondary button (visually subordinate), "Copy to clipboard" ghost button right side always enabled. Both download buttons disabled (40% opacity, `pointer-events: none`) until review checkbox is checked; checking enables both immediately. Copy to clipboard copies full text of active tab's document. `InterrogatoriesBadge.tsx` — shown on Interrogatories tab only, count parsed from `requests[].items` length, neutral badge styling ≤ 25, red badge with "review limit" label (e.g., "27 — review limit") > 25; Claude's `review_notice` field displayed at top of Interrogatories document when limit exceeded. "Back to edit" button top-left of Page 3, returns to `/input` with all SessionContext state intact. "Start new session" text link in header on Pages 2 and 3, clicking shows confirmation "This will clear your current session. Continue?" before clearing.
  Acceptance: Document renders with correct legal structure. Review checkbox gates both download buttons. Badge count is accurate and color changes at 25. Back to edit returns to Page 2 with form fully populated. Start new session confirmation works. Copy to clipboard copies active document text.
  Verify: Generate a real document. Verify document structure in viewer. Verify download buttons are disabled until checkbox checked. Verify badge count. Click Back to edit — confirm all fields are intact on Page 2.

- [ ] **8. Document export**
  Spec ref: `spec.md > Document Generation > .docx Assembly` and `spec.md > Document Generation > Download All (.zip)`
  What to build: `lib/buildDocx.ts` — takes a post-replace `GeneratedDocument` object, builds a Word document using the `docx` npm library with correct structure: caption block (centered, Texas court format), document title, To line, cover paragraph, definitions section (Interrogatories and RFP only — `definitions` array from JSON), numbered requests organized under bold section headings (from `requests[].section` and `requests[].items`), `{SignatureBlock}` placeholder (attorney completes in Word), certificate of service. Returns a `Blob` for browser download. Document must be fully editable in Word. `lib/buildZip.ts` — takes all `GeneratedDocument` objects, calls `buildDocx` for each, packages into a zip using `jszip`. File naming: `Interrogatories.docx`, `Requests_for_Production.docx`, `Requests_for_Admissions.docx`, `Requests_for_Disclosure.docx`. Wire both utilities to `DownloadControls.tsx` — "Download [Type]" triggers `buildDocx` for the active tab, "Download all (.zip)" triggers `buildZip`.
  Acceptance: "Download [Type]" downloads a properly formatted `.docx` for the active tab. "Download all (.zip)" downloads a zip containing one `.docx` per generated document type with correct file names. Files open in Word and are fully editable. Document structure matches spec: caption, numbered requests with bold headings, `{SignatureBlock}` placeholder, certificate of service.
  Verify: Download a single `.docx` and the full `.zip`. Open each `.docx` in Word — confirm caption format, numbered requests, bold section headings, `{SignatureBlock}` placeholder, and certificate of service.

- [ ] **9. Loading & error states**
  Spec ref: `spec.md > Page 3 — Output > Loading State` and `spec.md > Page 3 — Output > Inline Error States`
  What to build: `components/shared/LoadingSkeleton.tsx` — tabs pre-rendered with correct labels before content arrives (one per selected request type); skeleton cards pulse with document structure (title area, paragraph line blocks, section label placeholders, download button outlines); Elicit logo animation centered above skeleton: cyan particles (`#A3DDF2`) drift upward at varying speeds and opacity, loop until API responds, then fade out; wait time estimate below animation: "This usually takes 8–15 seconds."; on API response, skeleton fades out and real document content fades in — smooth CSS transition, no flash. `components/shared/ErrorState.tsx` — replaces skeleton on error, three inline states: rate limit (429) → "Too many requests. Please wait a moment and try again." + Try Again button; session limit → "You've reached the session limit. To start a new case, refresh the page." + no retry; API/network error → "Something went wrong while drafting your requests. Please try again." + Try Again button; if same API error occurs twice in a row, add second line: "If the problem continues, check your connection or try again shortly." Try Again re-triggers the identical API call with no re-entry required. Wire loading state to activate on Generate click (`isLoading: true` in SessionContext) and resolve on response or error.
  Acceptance: Loading skeleton shows with correct tab labels during API call. Particle animation plays and stops on resolve. Skeleton fades to content on success. Each error state renders with the correct message and correct action. Try Again re-triggers the API call. Session limit error shows no retry option. Form data in SessionContext is never cleared by an error.
  Verify: Generate and observe loading skeleton and animation. Disconnect network mid-request and confirm API error state. Trigger session limit (reach 10 calls) and confirm no retry is shown.

- [ ] **10. Submit your project to Devpost**
  Spec ref: `prd.md > What We're Building`
  What to build: Navigate to devpost.com and complete the submission form. Project name: **Elicit**. Tagline: **"Texas discovery requests, drafted in seconds."** Project description — use this core story verbatim: "Texas family law attorneys spend hours drafting discovery requests that follow predictable legal patterns — the same interrogatories about income, custody, and conduct, reformatted case after case. Elicit reads the facts of a specific case and drafts targeted, court-ready discovery requests from scratch — not filled-in templates, but requests written for that case, grounded in the Texas Rules of Civil Procedure and the Texas Family Code. The attorney reviews, downloads a Word document, and serves it — the AI handled the drafting, the attorney owns the work product." Built-with tags: Next.js, React, TypeScript, Tailwind CSS, Bun, Cloudflare Pages, Anthropic Claude API, docx, jszip. Primary screenshot: Page 3 with a fully loaded document visible in the viewer, tabs showing multiple request types, and download controls at the bottom. Upload docs/ artifacts: `scope.md`, `prd.md`, `spec.md`, `checklist.md`. Link GitHub repo: https://github.com/Mistiblu78/elicit. Optionally deploy to Cloudflare Pages and add the live URL. Review all fields and submit.
  Acceptance: Submission is live on Devpost with project name, tagline, description, built-with tags, Page 3 hero screenshot, docs artifacts, and repo link. All required fields are complete.
  Verify: Open the Devpost submission page and confirm the green "Submitted" badge appears. Read the project description — would a judge who knows nothing about Elicit understand what it does and why it matters?
